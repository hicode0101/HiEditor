# HiEditor 插件开发指南

> 适用版本：apiVersion = 1（宿主常量 `HI_PLUGIN_API_VERSION`）
> 本文与代码实现严格对齐：标注了每个能力的**当前实现状态**，未实现/预留的能力会明确说明。
> 配套需求文档：[docs/HiEditor-功能开发需求文档.md](docs/HiEditor-功能开发需求文档.md)（附录 C 为本指南的规范来源）

---

## 1. 插件能做什么（能力总览）

HiEditor 采用"宿主 + 插件"架构：主程序为宿主，格式能力的扩展通过插件接入。插件目前可以贡献以下能力：

| 能力 | 贡献方式 | 宿主消费点 | 状态 |
|---|---|---|---|
| 注册语言（扩展名 → 语言） | config.json `languages[]` | 状态栏语言显示、打开文件自动识别、文档类型联动 | ✅ 已实现 |
| 语法高亮 | 语言注册后由前端 CM 语言包渲染 | 源码编辑区 | ⚠️ 部分（见 §7） |
| 格式化命令 | config.json `formatters[]` + 原生命令 | 编辑菜单、编辑区右键菜单、快捷键 | ✅ 已实现 |
| Markdown 工具栏 / 所见即所得 | config.json `toolbar` 声明驱动 | B 区工具栏整组切换 | ✅ 工具栏（`toolbar: {id}` 已消费）/ ⏳ WYSIWYG 引擎接入中 |
| 个性化设置项 | config.json `settings[]` | 设置 ▸ 插件 | ⏳ 规划中（字段已解析忽略） |
| 前端资源（UI 扩展） | `frontend` 字段 | — | ⏳ 预留（未加载） |
| 自定义工具栏容器 | `toolbar: { id }` + 前端预置 `#toolbar-{id}` 容器 | 语言激活时 B 区整组切换 | ✅ 已实现（v1.6，见 §4） |

**内置插件**（随安装包附带于 `plugins\`，可作为开发参考）：

| 插件 | id | 能力 |
|---|---|---|
| markdown | `hieditor.markdown` | Markdown 语言、工具栏切换、WYSIWYG（引擎接入中） |
| json | `hieditor.json` | JSON 语言 + 格式化/压缩命令 |
| xml | `hieditor.xml` | XML 语言 + 格式化/压缩命令 |
| plaintext | `hieditor.plaintext` | 纯文本兜底 |
| code | `hieditor.code` | HTML/CSS/JS/TS/Python/Java/C#/C/C++/Go/Rust/SQL/YAML/Shell/INI-TOML/Batch 语言注册 |

---

## 2. 快速上手：10 分钟写一个格式化插件

以一个"文本大写转换"插件为例（对 `.txt` 生效的演示插件）。

### 2.1 创建目录与文件

```
plugins/
└─ upper/
   ├─ config.json
   ├─ src/
   │  └─ lib.rs
   └─ Cargo.toml
```

### 2.2 编写 config.json

```json
{
  "id": "hieditor.upper",
  "name": "大写转换",
  "version": "1.0.0",
  "apiVersion": 1,
  "enabled": true,
  "author": "your-name",
  "description": "将选区/全文转换为大写（演示插件）",
  "entry": {
    "windows": "bin/hieditor_upper.dll",
    "linux": "bin/hieditor_upper.so",
    "macos": "bin/hieditor_upper.dylib"
  },
  "languages": [
    { "id": "plaintext", "name": "纯文本", "extensions": [".txt"], "highlight": false }
  ],
  "formatters": [
    { "id": "upper.demo", "label": "转大写", "language": "plaintext", "command": "upper" }
  ]
}
```

### 2.3 编写 Cargo.toml

```toml
[package]
name = "hi-plugin-upper"
version = "0.1.0"
edition = "2021"

[lib]
name = "hieditor_upper"
crate-type = ["cdylib"]

[dependencies]
hi-editor-plugin-abi = { path = "../../crates/hi-editor-plugin-abi" }
```

> `[lib] name` 决定 dll 文件名，必须与 `entry.windows` 中的文件名一致。

### 2.4 编写 lib.rs

```rust
use hi_editor_plugin_abi::hi_export_plugin;

// 命令处理函数：宿主把 cmd/text/opts 传进来，返回处理结果或错误
fn run(cmd: &str, text: &str, _opts: &str) -> Result<String, String> {
    match cmd {
        "upper" => Ok(text.to_uppercase()),
        other => Err(format!("未知命令：{other}")),
    }
}

hi_export_plugin!("hieditor.upper", "1.0.0", run);
```

### 2.5 编译与部署

```bash
cargo build --release
cp target/release/hieditor_upper.dll plugins/upper/bin/
```

（Windows 下双击 `build-release.bat` 会自动完成全部插件的编译与同步。）

重启 HiEditor 后：打开任意 `.txt` 文件 → 编辑菜单底部出现"转大写"，编辑区右键菜单"格式化"同样可用；状态栏语言显示"纯文本"。

---

## 3. 插件目录结构

```
plugins/                                ← 插件根目录（默认 = 安装目录下）
└─ <插件名>/                            ← 每个插件一个独立文件夹
   ├─ config.json                       ← 必需：插件清单
   ├─ bin/
   │  ├─ hieditor_<名>.dll              ← Windows 原生扩展（entry.windows 指向）
   │  ├─ hieditor_<名>.so               ← Linux
   │  └─ hieditor_<名>.dylib            ← macOS
   └─ assets/                           ← 可选：前端资源（当前版本未加载，预留）
```

**宿主扫描的目录顺序**（全部扫描，先注册者优先）：

1. 环境变量 `HIEDITOR_PLUGINS_DIR`（若设置）
2. `<exe 所在目录>\plugins`
3. `<exe 所在目录>\..\..\plugins`（开发态工作区布局）
4. Tauri 资源目录下 `plugins`
5. 当前工作目录 `plugins`

同一插件 id 在多个目录出现时，**扫描顺序靠前的生效**；同目录内按**文件夹名字典序**加载。

---

## 4. config.json 字段参考

必填字段：`id`、`name`、`version`、`apiVersion`、`enabled`、`entry`；其余可选。未知字段会被忽略（向前兼容）。

| 字段 | 类型 | 必填 | 说明 | 宿主当前消费 |
|---|---|---|---|---|
| `id` | string | ✅ | 全局唯一标识，约定 `hieditor.插件名` | ✅ 设置页展示、命令路由 |
| `name` | string | ✅ | 显示名称（zh-CN） | ✅ 设置页/语言菜单 |
| `version` | string | ✅ | 插件版本（semver） | ✅ 设置页展示 |
| `apiVersion` | number | ✅ | 必须等于 `1`，否则拒绝加载 | ✅ 加载前校验 |
| `enabled` | bool | ✅ | **加载开关**：true 自动加载；false 忽略（不加载 dll） | ✅ 设置页可改，写回本文件，重启生效 |
| `author` / `description` | string | — | 作者 / 描述 | 设置页展示 |
| `entry` | object | ✅ | 各平台动态库相对路径：`windows` / `linux` / `macos` | ✅ 加载阶段使用 |
| `frontend` | string | — | 前端脚本相对路径 | ⏳ 预留（当前未加载） |
| `languages[]` | array | — | `{ id, name, extensions[], highlight }` | ✅ 语言注册与识别 |
| `formatters[]` | array | — | `{ id, label, language, command, shortcut? }` | ✅ 菜单/右键/快捷键 |
| `menus` | object | — | `{ context: ["format", …] }` | ⏳ 预留（右键菜单当前按 formatters 是否存在自动显示） |
| `toolbar` | object | — | 自定义工具栏声明 `{ id }`（v1.6 已消费，见下） | ✅ 语言 → 工具栏联动 |
| `preview` | object | — | 编辑视图声明 `{ engine }` | ⏳ 预留 |
| `viewControls[]` | array | — | 视图控件声明（如 `fontFamily`/`fontSize`） | ⏳ 预留（字体/字号栏已收归宿主默认，不再依赖插件声明） |
| `settings[]` | array | — | 个性化配置项 | ⏳ 规划中 |

> `toolbar` 字段（v1.6 起消费）：声明 `"toolbar": { "id": "xxx" }` 后，本插件 `languages[]` 声明的每种语言激活时，B 区显示前端预置的 `#toolbar-xxx` 容器（未声明该字段的语言显示宿主默认的字体/字号工具栏）。容器 HTML/交互由前端实现（见 `ui/index.html` 与 `ui/src/toolbar.js`），插件只负责声明语言与工具栏的绑定关系。禁用或加载失败时绑定自动失效（回落默认工具栏）。

---

## 5. 原生扩展开发（C ABI）

### 5.1 必须导出的函数

使用 `hi-editor-plugin-abi` crate 提供的 `hi_export_plugin!` 宏一行导出全部符号：

```rust
use hi_editor_plugin_abi::hi_export_plugin;

fn run(cmd: &str, text: &str, opts: &str) -> Result<String, String> {
    // cmd  = formatters[].command（如 "pretty"）
    // text = 当前文档全文（或选区内容）
    // opts = JSON 字符串，如 {"indent":4}（设置 ▸ 文本编辑 ▸ 格式化缩进）
    ...
}

hi_export_plugin!("hieditor.插件名", "1.0.0", run);
```

宏展开后导出的符号（与附录 C.4 一致）：

| 符号 | 作用 |
|---|---|
| `hi_plugin_meta() -> HiPluginMeta` | 返回 id/version/apiVersion（宿主加载前校验） |
| `hi_plugin_api_version() -> u32` | 返回 API 大版本 |
| `hi_plugin_init() -> i32` | 初始化，返回 0 表示成功 |
| `hi_plugin_command(cmd, text, opts) -> *mut c_char` | 执行命令，返回 JSON 字符串 |
| `hi_plugin_free_string(s)` | 归还命令结果字符串的内存 |
| `hi_plugin_shutdown() -> i32` | 卸载前清理 |

### 5.2 命令返回协议

`hi_plugin_command` 必须返回 JSON 字符串（宏已自动包装与转义）：

```json
{ "ok": true,  "text": "处理后的文档内容" }
{ "ok": false, "error": "第 3 行, 第 5 列：缺少引号" }
```

- `ok=true` 时宿主用 `text` **整体替换**目标文本（一步可撤销）；
- `ok=false` 时宿主弹出错误对话框（错误文本中建议带行列定位，JSON 插件的实现可参考）；
- 宿主对命令调用施加 **10 秒超时**（规划中，当前版本尚未强制）。

### 5.3 约束

1. **禁止 panic 外泄**：宏内部已用 `catch_unwind` 包裹，panic 会被转为错误结果，但请勿依赖此行为兜底逻辑错误。
2. **不要在 `hi_plugin_init` 中做耗时操作**（会拖慢启动）。
3. **不要在命令中直接操作 UI/文件系统**——一切经宿主传入的数据与返回值交互。
4. 动态库必须为 **稳定 C ABI**（`crate-type = ["cdylib"]`），不要导出 Rust 特有类型。

---

## 6. 加载流程与生命周期

```
启动
 └─ 扫描 plugins/* 子文件夹（按文件夹名字典序）
     ├─ 无 config.json           → 忽略
     ├─ config.json 解析失败      → 标记"加载失败"（原因可见），继续
     ├─ enabled = false          → 跳过（不加载 dll），标记"已停用"
     ├─ apiVersion 不匹配        → 标记"加载失败"
     ├─ entry 文件缺失/加载失败    → 标记"加载失败"
     └─ 成功                     → 调用 hi_plugin_init / 注册贡献 → 标记"已加载"
```

- **单个插件失败不影响宿主与其它插件**（隔离原则，FR-18.5）。
- 每次启动时若检测到插件加载失败，会显示一次横幅提示。
- 卸载即退出进程；无运行中热卸载。

### 插件停用/失败时的降级矩阵

| 停用的插件 | 回退行为 |
|---|---|
| markdown | `.md` 以纯文本打开：无 WYSIWYG、无 Markdown 工具栏、无高亮 |
| json / xml | 对应文件不着色，格式化/压缩菜单与快捷键消失 |
| code | 代码类文件不着色（仍可编辑） |
| 全部停用 | 编辑器退化为纯文本编辑器，界面框架不变 |

---

## 7. 语言与语法高亮接入

1. 在 `languages[]` 声明语言与扩展名 → 打开匹配文件时状态栏显示语言名、文档类型联动（Markdown 专属）。
2. `highlight: true` 表示"该语言应当高亮"。**当前实际高亮由前端 CodeMirror 6 语言包提供**，已接入的语言：

| 语言 id | 高亮 | 语言 id | 高亮 |
|---|---|---|---|
| markdown | ✅ | go | ⏳ 待接入 |
| json | ✅ | rust | ⏳ 待接入 |
| xml | ✅ | sql / yaml / shell / ini / batch | ⏳ 待接入 |
| html / css / js / ts / python / java / c# / c++ | ⏳ 待接入 | | |

3. 未接入高亮语言包的语言：正常编辑、状态栏正常显示语言名，仅无着色。
4. 高亮配色：浅色主题见需求文档 FR-17.4 色表；深色主题配色在 `ui/src-cm/cm-entry.js` 的 `darkHighlight`。

---

## 8. 编译与部署

### 8.1 一键脚本（Windows，推荐）

项目根目录的 **`build-release.bat`**：

```text
[1/3] cargo build --release        ← 编译主程序 + 全部插件 dll
[2/3] 同步 plugins\*\bin\          ← 结束运行中的实例并复制 dll
[3/3] 询问是否立即启动
```

### 8.2 手动步骤

```bash
cargo build --release
cp target/release/hieditor_json.dll plugins/json/bin/
cp target/release/hieditor_xml.dll plugins/xml/bin/
cp target/release/hieditor_markdown.dll plugins/markdown/bin/
cp target/release/hieditor_plaintext.dll plugins/plaintext/bin/
cp target/release/hieditor_code.dll plugins/code/bin/
```

> Linux/macOS 将 `.dll` 换成 `.so` / `.dylib`。注意：**正在运行的实例会锁定 dll**，复制前需先退出应用。

### 8.3 部署到用户机器

- 将整个 `plugins\` 目录随安装包分发（内置插件默认如此）；
- 第三方插件：把插件文件夹**整体拷贝**到 `plugins\` 下即可，重启后生效；
- 插件启用/停用：设置 ▸ 插件 ▸ 开关（写回该插件 `config.json` 的 `enabled`，**重启生效**）。

---

## 9. 调试与故障排查

**设置 ▸ 插件** 中每个插件显示状态：

| 状态 | 含义 | 常见原因 |
|---|---|---|
| 已加载 | dll 加载成功、贡献已注册 | — |
| 已停用 | `enabled=false`，未加载 dll | 开关被关闭 |
| 加载失败 | dll 缺失/损坏、apiVersion 不匹配、init 失败 | 重新编译/检查 entry 路径/核对 apiVersion |

**常见问题对照**：

| 症状 | 原因 | 处理 |
|---|---|---|
| 设置里看不到我的插件 | 文件夹下没有 `config.json`，或 JSON 语法错误 | 检查文件与语法 |
| 插件显示"加载失败：入口文件不存在" | `entry` 路径与实际 dll 文件名/位置不符 | 核对 `bin\` 下文件名 |
| 插件显示"加载失败：apiVersion 不匹配" | `apiVersion` ≠ 宿主的 1 | 升级插件或宿主 |
| 菜单里没有"格式化" | 当前语言没有可用 formatter | 检查 `formatters[].language` 与状态栏语言是否一致 |
| 状态栏语言不对 | 扩展名未被任何插件注册 | 在状态栏语言菜单手动覆盖，或补充 `languages[].extensions` |

开发期建议用 **debug 构建**（`cargo build`）运行——命令行窗口会保留 panics/日志输出；正式使用用 **release**（无命令行）。

---

## 10. 版本兼容与约定

- 插件 id 约定格式：**`hieditor.插件名`**（内置插件均如此命名），需全局唯一。
- `apiVersion = 1` 为当前宿主 ABI 版本；宿主升级 ABI 时会递增，旧插件将被拒绝加载并提示。
- 文本协议：命令结果 JSON `{"ok":bool, "text"|"error":string}`，编码 UTF-8。
- 设置 ▸ 插件 中修改开关后**必须重启**才生效（热卸载为后续版本规划）。

---

## 11. 已知限制（截至本文档对应版本）

1. 命令级**超时保护**与 **panic 后的插件级禁用**尚未实现（规划中）；
2. `frontend` / `preview` / `settings` 字段已解析但宿主行为未接入（`toolbar` 自 v1.6 起已消费：语言 → 自定义工具栏联动）；
3. 用户自定义插件目录（`用户数据目录\plugins`）为 P1 规划；
4. 第三方插件签名校验与沙箱为 P2；
5. `code` 插件注册的语言中，HTML/CSS/Python 等的**高亮**将随前端 CM 语言包逐步接入（当前仅注册语言，不着色）。
