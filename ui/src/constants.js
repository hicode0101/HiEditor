// 全局常量定义：应用标识、版本号等不随运行状态变化的固定值统一放这里。
// 需要调整时只改本文件，其它模块通过 import 引用，避免多处硬编码漏改。

export const APP_NAME = "HiEditor";
export const APP_VERSION = "1.0.3";
// 名称+版本组合（关于页等处显示）
export const APP_NAME_VERSION = `${APP_NAME} ${APP_VERSION}`;
